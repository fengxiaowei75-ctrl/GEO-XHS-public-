# .cursorrules — GEO-XHS 项目代码规范

## 文件大小红线
- 新增文件默认不超过 500 行。
- 工具函数文件建议 ≤ 150 行；UI 组件建议 ≤ 250 行；页面级 View 建议 ≤ 400 行。
- 现有超过 500 行的 legacy 文件不要求立即拆分，不能为了行数达标做无业务收益的大拆分。
- 修改 legacy 大文件时，禁止继续无边界追加大块逻辑；优先把新增逻辑抽到新的 helper、hook、component 或 API util。
- 拆分必须小步进行，单次拆分只移动一类职责，不改变外部行为。
- 拆分前后必须补齐/运行相关验证，尤其要覆盖 lazy view 首屏渲染、漏 import、未声明变量和线上入口行为。

## 新增功能的目录规则
- 纯函数 → utils/
- 可复用 UI → components/{layout,data,form,charts}/
- 页面 → views/xxx.jsx（独立文件）
- 状态逻辑 → hooks/xxx.js
- API 封装 → api/_utils.js

## 禁止行为
- 不许在已有大文件里直接追加代码
- 不许用 "latest" 作为依赖版本
- 不许把业务逻辑塞进 App.jsx
- 不许为了满足行数红线而做缺少测试覆盖的大规模重排

## 工作模式
1. 用户说"加功能" → 先判断功能类型 → 选择正确目录
2. 打开目标文件 → 检查行数 → 超过建议线时先判断是否有真实职责边界可抽取
3. 如果是 legacy 大文件，优先新增小模块承接新逻辑；只有在测试覆盖足够时才拆历史代码
4. 提交前检查：
   - npm test
   - npm run build
   - npm run test:e2e
   - no-undef 静态扫描
   - 相关页面实际打开验证
