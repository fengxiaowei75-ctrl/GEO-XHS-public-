# .cursorrules — GEO-XHS 项目代码规范

## 文件大小红线（硬限制）
- 工具函数文件：≤ 100 行
- UI 组件：≤ 250 行
- 页面级 View：≤ 400 行
- 任何文件：≤ 500 行（超过必须拆分）

## 新增功能的目录规则
- 纯函数 → utils/
- 可复用 UI → components/{layout,data,form,charts}/
- 页面 → views/xxx.jsx（独立文件）
- 状态逻辑 → hooks/xxx.js
- API 封装 → api/_utils.js

## 禁止行为
- 不许在已有大文件里直接追加代码
- 不许用 "latest" 作为依赖版本
- 不许把业务逻辑塞进 App.jsx

## 工作模式
1. 用户说"加功能" → 先判断功能类型 → 选择正确目录
2. 打开目标文件 → 检查行数 → 超过 300 行则建议拆分
3. 提交前检查：npm run build 必须通过
